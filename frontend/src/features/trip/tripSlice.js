import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const tripSlice = createSlice({
  name: "trip",
  initialState,
  reducers: {},
  extraReducers: {},
});

export default tripSlice.reducer;
